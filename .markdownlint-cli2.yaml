# Default state for all rules
default: true

# Disable some built-in rules
config:
  line-length:
    tables: false

# CLAUDE.md files are single-line `@AGENTS.md` import pointers — nothing to lint.
ignores:
  - "CLAUDE.md"
  - "**/CLAUDE.md"
