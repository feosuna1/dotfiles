[alias]
	# My hold overs from svn days
	st = status -s
	ci = commit
	co = checkout
	checkpoint = !git commit -m \"Checkpoint-$(date +'%Y-%m-%d %H:%M:%S')\" --no-verify

	# Retrieve everything from all remotes, pruning local branches
	fetch-all = fetch --all --prune

	# List aliases.
	aliases = !git config --get-regexp alias | sort

	# Amend the currently staged files to the latest commit.
	amend = commit --amend --no-edit

	# Show the user email for the current repository.
	whoami = config user.email

	# Constant rebasing causes a bunch of unreachable commit. This removes
	# those commits (https://stackoverflow.com/a/37335660)
	remove-unreachable-commits = !git reflog expire --expire-unreachable=now --all --updateref --rewrite --stale-fix && git gc --aggressive --prune=now

	# Edit my configs!
	editconfig = !$EDITOR ~/.dotfiles/files/config/git/config &

	# Pretty print my log
	lg = log --graph --decorate --all --oneline

	# Push, only if we're not overwriting what's already there.
	safe-force-push = push --force-with-lease

	# Rebase with autosquash, without editing the todo file, ex. autosquash <from-head>
	autosquash = -c core.editor=true rebase -i --autosquash --autostash

	# Clean repo ignoring xcworkspace
	xcclean = clean -e "*.xcworkspace/" -e "LocalConfig"

	# Get the sha of the symbolic reference, or the current HEAD if no ref is given.
	#
	# The `&& :` part tells the shell to run the `:` (no-op) command after your main command
	# succeeds, which consumes any remaining arguments that git would otherwise append.
	sha = !git rev-parse ${1:-HEAD} | tr -d '\n' && :

	# Mirror to a backup repo
	bak = "!f(){ \
		git config remote.bak.url > /dev/null && git push --mirror bak || \
		echo \"ERROR: 'bak' is missing. Run:\n\n    git config-bak <PATH>\n\"; \
	};f"

	# Configure a backup repo
	config-bak = remote add --mirror=push bak

	# Cherry pick for release
	release-pick = cherry-pick -m1

[apply]
	# Detect whitespace errors when applying a patch.
	whitespace = fix

[branch]
	# controls whether new branches should be set up to be rebased upon git pull
	autosetuprebase = always

	# Sorts branches by committer date (see https://blog.gitbutler.com/git-tips-2-new-stuff-in-git/)
	sort = -committerdate

[branchless "revsets.alias"]
	this = stack()

[core]
	# Use custom `.gitignore`
	excludesfile = ~/.dotfiles/files/config/git/ignore

	# Treat spaces before tabs and all kinds of trailing whitespace as an error.
	# [default] trailing-space: looks for spaces at the end of a line
	# [default] space-before-tab: looks for spaces before tabs at the beginning of a line
	whitespace = space-before-tab,-indent-with-non-tab,trailing-space

	# Make `git rebase` safer on macOS.
	# More info: http://www.git-tower.com/blog/make-git-rebase-safe-on-osx/
	trustctime = false

	# Prevent showing files whose names contain non-ASCII symbols as unversioned.
	# http://michael-kuehnel.de/git/2014/11/21/git-mac-osx-and-german-umlaute.html
	precomposeunicode = false

	## `git branch` is difficult to use with less (see https://stackoverflow.com/a/49862151).
	## This makes interactions that require paging a bit more reasonable.
	## pager = less -FRX
	## pager = "diff-so-fancy | less --tabs=4 -RFX" # nicer diffs
	pager = delta

	# Improve Git monorepo performance with a file system monitor
	# (https://github.blog/2022-06-29-improve-git-monorepo-performance-with-a-file-system-monitor/)
	fsmonitor = true
	untrackedcache = true

	# Replaces CRLF to LF if they get introduced
	autocrlf = input

[delta]
    navigate = true  # use n and N to move between diff sections
    dark = true      # or light = true, or omit for auto-detection
    side-by-side = true
	line-numbers = true

[diff]
	# Detect copies as well as renames.
	renames = copy

	# Better diffing algorithm for tracking better / more context.
	algorithm = histogram

[diff "bin"]
	# Use `hexdump` to diff binary files.
	textconv = hexdump -v -C

[difftool]
	prompt = false
	trustExitCode = true

[init]
	templatedir = ~/.dotfiles/files/config/git/template
	defaultBranch = develop

[interactive]
	diffFilter = delta --color-only

[log]
	# Display dates as 2023-05-25 13:54:51 instead of Thu May 25 13:54:51 2023
	date = iso

[merge]
	# Shows old, new, and ancestor when highlighting a conflict
	conflictstyle = zdiff3

[mergetool]
	prompt = false

[push]
	# Pushes the current branch to the remote, even if it is not tracked.
	default = current

[pull]
	# Only allow fastforward merges https://docs.gitlab.com/ee/user/project/merge_requests/methods/#fast-forward-merge
	ff = only

[rebase]
	# Squashes `--fixup` when rebasing.
	autosquash = true

	# Automatically stashes changes before running a rebase.
	autostash = true

[rerere]
	enabled = true

[user]
	name = Enrique Osuna
	email = 12620+feosuna1@users.noreply.github.com

[url "git@github.com:"]
	pushInsteadOf = "https://github.com/"

[bash]
	showInformativeStatus = true
	showDirtyState = true
	showUntrackedFiles = true
